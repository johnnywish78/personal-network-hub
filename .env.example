# JPNH - Johnny Personal Network Hub
# Copy to .env and fill in. Never commit .env to git.
#
# Secrets are NOT stored in this file. They are entered through the
# application UI and saved to the local secure vault
# (~/.jpnh/secrets.json with restrictive permissions).

# --- Local storage locations (leave default) ---
JPNH_DATA_DIR=~/.jpnh
